# =============================================================================
# Warungio Marketplace — Default Nginx Site Override
# =============================================================================
# This file is intentionally left blank to override and disable the default
# Nginx virtual host configuration shipped inside the official Nginx container.
#
# By mounting this empty file to /etc/nginx/conf.d/default.conf:ro, we prevent
# default server conflicts and ensure only warungio.conf serves traffic on port 80.
# =============================================================================
